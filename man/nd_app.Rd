% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nd_app.R
\name{nd_app}
\alias{nd_app}
\title{nd_app}
\usage{
nd_app(..., .title = "EnDiKaU", .lang = "de", .head = NULL)
}
\arguments{
\item{...}{App-Inhalt; wird direkt in den \verb{<body>} gelegt. Die Breite regelt
beim Einbetten als iframe der iframe-resizer (child), daher kein eigener
Layout-Container.}

\item{.title}{Seitentitel (\verb{<title>}).}

\item{.lang}{Sprachcode für das \verb{<html>}-Element.}

\item{.head}{Optionale, app-spezifische \verb{<head>}-Inhalte (z. B. eigene
Styles/Skripte), als Tag oder \code{tagList}.}
}
\value{
Ein \verb{<html>}-\code{shiny.tag}.
}
\description{
Seitengerüst (Container) für interaktive Apps, z. B. Shiny. Ersetzt den
bisherigen Aufruf \code{nd_page(.page_type = "app", .navbar = NULL, .main = ...)}.
}
\details{
Geladen werden dieselben gemeinsamen Assets wie im \code{head} des Hugo-Themes
(\verb{nd.site/_hugo/themes/endikau_theme/layouts/partials/head.html}) – jedoch
ohne Site-Chrome: kein Headroom, kein TOC, kein Masonry, kein Matomo-Tracking
und kein Wordcloud (wordcloud2/d3/d3-cloud). iframe-resizer wird bewusst als
\strong{child} geladen, da eine App typischerweise selbst im iframe eingebettet
ist. Alle Pfade liegen unter \code{assets/...} und setzen voraus, dass dieses
Verzeichnis bereitsteht (in Shiny via
\verb{shiny::addResourcePath("assets", <nd_assets>/dist)}).
}
\examples{
NULL
}
